# Math Mentor AI - Streamlit Cloud Compatible
# Only truly required + key optional packages that install cleanly

# Core UI
streamlit>=1.30.0,<2.0.0
streamlit-option-menu>=0.3.6

# Required (bare imports in code)
python-dotenv>=1.0.0
numpy>=1.24.0
Pillow>=10.0.0
requests>=2.31.0

# AI / LLM (optional in code, but needed for functionality)
groq>=0.4.1
google-generativeai>=0.3.2

# Math (used inside try/except in mcp_integration.py)
sympy>=1.12

# Input Processing (optional, wrapped in try/except)
pytesseract>=0.3.10
pydub>=0.25.1

# Web Search (optional, wrapped in try/except)
duckduckgo-search>=5.0